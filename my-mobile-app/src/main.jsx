import React from "react";
import ReactDOM from "react-dom/client";
import Splash from "./pages/Splash"; // make sure the path is correct
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Splash />
  </React.StrictMode>
);
